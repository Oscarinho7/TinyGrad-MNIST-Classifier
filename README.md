# TinyGrad-MNIST-Classifier
An end-to-end Deep Learning project using tinygrad to train MLP and CNN models on MNIST, then deploy them in a WebGPU-powered web app. Users can draw digits on a canvas, get real-time predictions, and see model confidence visualizations directly in the browser.
